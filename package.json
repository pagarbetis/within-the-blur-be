{
  "name": "within-the-blur-be",
  "version": "1.0.0",
  "description": "Within the Blur — Backend API (Fastify + Prisma + MySQL)",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@fastify/cookie": "^9.4.0",
    "@fastify/cors": "^9.0.1",
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "dotenv-expand": "^13.0.0",
    "fastify": "^4.28.1",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.22.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.3"
  }
}
